import React from 'react';
import {Contact} from '../Pages/Contact'

const ContactComp = () => {
    return (
        <div>
            <Contact/>
        </div>
    );
};

export default ContactComp;